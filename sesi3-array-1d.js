var peserta = ['abdul', 'nur', 'bram', 'iqbal', 'drajad', 'rebecca', 'david']

//jumlah data
console.log( peserta.length )
console.log( '============' )

//mengakses data satuan dari array
console.log( peserta[0] )
console.log( '============' )

//mengakses semua data dari array
for (let i=0; i < peserta.length; i++) {
    console.log( peserta[i] )
}
console.log( '============' )
